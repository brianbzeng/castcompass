# Structure reference image sources

The canonical human-readable source table is
[`structure-image-audit.md`](structure-image-audit.md). Its machine-readable authority is
[`governance/authorship-provenance.json`](../governance/authorship-provenance.json), and the live
attribution copy is [`app/data/structure-images.json`](../app/data/structure-images.json).

Do not add or replace a public image by editing only the UI. Update the register, local SHA-256,
source evidence, direct license link, modification disclosure, and live data together; the
authorship/provenance verifier fails closed when they drift.

No suitable reviewed reference image is registered yet for cove, gravel slope, protected bay,
protected cove, shelf break, or slope break.
